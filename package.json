{
  "name": "lostlink-fyp",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend,admin -c blue,green,magenta \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" \"npm run dev --prefix admin\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
